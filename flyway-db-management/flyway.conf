# Flyway Configuration for Aurora Postgres with LocalStack
# This configuration simulates AWS Aurora Postgres environment using LocalStack

# Database connection settings
flyway.url=jdbc:postgresql://localhost:5432/aurora_postgres_db
flyway.user=postgres
flyway.password=postgres

# Flyway settings
flyway.locations=filesystem:scripts/ddl,filesystem:scripts/dml
flyway.baselineOnMigrate=true
flyway.validateOnMigrate=true
flyway.outOfOrder=false
flyway.cleanDisabled=false

# Schema management
flyway.schemas=public,audit,logs
flyway.defaultSchema=public

# Migration settings
flyway.table=schema_version
flyway.sqlMigrationPrefix=V
flyway.sqlMigrationSeparator=__
flyway.sqlMigrationSuffixes=.sql

# AWS Aurora specific settings (simulated with LocalStack)
flyway.connectRetries=3
flyway.connectRetriesInterval=10

# Logging
flyway.loggers=slf4j
